{% extends 'base.html.twig' %}

{% block title %}Iteneraire{% endblock %}

{% block body %}
<main id="main" class="main">
    <h1>Iteneraire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ iteneraire.id }}</td>
            </tr>
            <tr>
                <th>Pts_depart</th>
                <td>{{ iteneraire.ptsDepart }}</td>
            </tr>
            <tr>
                <th>Pts_arrive</th>
                <td>{{ iteneraire.ptsArrive }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_iteneraire_index') }}">back to list</a>

    <a href="{{ path('app_iteneraire_edit', {'id': iteneraire.id}) }}">edit</a>

    {{ include('iteneraire/_delete_form.html.twig') }}
    </main>
{% endblock %}
