{% extends 'base.html.twig' %}

{% block title %}Role{% endblock %}

{% block body %}
<div class="row" style="text-align: center;align-items: center;justify-content: center;margin: 100px 500px 30px;">   
    <h1>Role</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ role.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ role.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-outline-info" href="{{ path('app_role_index') }}">Retour</a>
    <a class="btn btn-outline-success" href="{{ path('app_role_edit', {'id': role.id}) }}">Modifier</a>

    {{ include('role/_delete_form.html.twig') }}
{% endblock %}
