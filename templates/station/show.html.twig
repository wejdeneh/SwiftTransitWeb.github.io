{% extends 'base.html.twig' %}

{% block title %}Station{% endblock %}

{% block body %}
    <h1 style=" margin-left:350px; margin-top:80px" >Station</h1>
   <div style="display: flex ;align-items: center;justify-content: center;margin-top:25px; margin-left:350px;margin-right:60px">

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ station.id }}</td>
            </tr>
            <tr>
                <th>Long_alt</th>
                <td>{{ station.longAlt }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_station_index') }}">back to list</a>

    <a href="{{ path('app_station_edit', {'id': station.id}) }}">edit</a>

    {{ include('station/_delete_form.html.twig') }}
    </div>
{% endblock %}
