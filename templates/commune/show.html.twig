{% extends 'base.html.twig' %}

{% block title %}Commune{% endblock %}

{% block body %}
    <h1 style=" margin-left:350px; margin-top:80px" >Commune</h1>
    <div style="display: flex ;align-items: center;justify-content: center;margin-top:100px; margin-left:350px;margin-right:60px">

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commune.id }}</td>
            </tr>
            <tr>
                <th>Nom_commune</th>
                <td>{{ commune.nomCommune }}</td>
            </tr>
            <tr>
                <th>Long_alt</th>
                <td>{{ commune.longAlt }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commune_index') }}">back to list</a>

    <a href="{{ path('app_commune_edit', {'id': commune.id}) }}">edit</a>

    {{ include('commune/_delete_form.html.twig') }}
    </div>
{% endblock %}
