{% extends 'base.html.twig' %}

{% block title %}UserState{% endblock %}

{% block body %}
    <h1>UserState</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_state.id }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ user_state.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_user_state_index') }}">back to list</a>

    <a href="{{ path('app_user_state_edit', {'id': user_state.id}) }}">edit</a>

    {{ include('user_state/_delete_form.html.twig') }}
{% endblock %}
